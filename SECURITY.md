# Security

This project runs a local Python HTTP server bound to `127.0.0.1` and modifies copies of local game files.

Please report security-sensitive problems privately rather than posting exploit details in a public issue when possible. Do not include passwords, tokens, personal filesystem information, or private account data in debug reports.

The mod does not need administrator privileges and should not request them.
